# my-AI-stro backend runtime dependencies.
#
# Curated by hand to the actual import surface — see
# `grep -rh "^import \|^from " backend/ --include='*.py'` for the real list.
# Previously this file was a `pip freeze` dump with 60+ packages; the
# extras were all dev tooling (black, ruff, mypy, pytest, twine) that
# crept in during development.
#
# Pins use compatible-release (`~=`) so security patches land but
# breaking changes don't. Bump deliberately.

fastapi~=0.136.0       # web framework + request routing
uvicorn~=0.46.0        # ASGI server (run via `uvicorn main:app --reload`)
pydantic~=2.13.0       # request/response validation + the event schema
ollama~=0.6.0          # local-LLM client (talks to the Ollama daemon)
